@model IEnumerable<GM_Shop.Models.MenuDanhMuc>
<div class="col-lg-12">
    <div class="mb-3">
        <h4>Danh mục</h4>
        <ul class="list-unstyled fruite-categorie">
            @foreach (var item in Model)
            {
                <li>
                    <div class="d-flex justify-content-between fruite-name py-1">
                        <a asp-controller="SanPham" asp-action="Index" asp-route-idCategory="@item.MADMSP">
                            <i class="fas fa-apple-alt me-2"></i> @item.TENDMSP
                        </a>
                        <span>(@item.Count)</span>
                    </div>
                </li>
            }
        </ul>
    </div>
</div> 